[project]
name = "stock-backfire"
version = "0.1.0"
description = "A股股票/ETF/大盘回测平台，用于验证投资策略"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "network: 需要联网的集成测试（无网时自动跳过）",
]
addopts = "-q"
