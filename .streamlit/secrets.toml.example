# 复制本文件为 secrets.toml 并填入真实值。
# 本地：放在项目根的 .streamlit/secrets.toml（已被 .gitignore 排除，不会上传）。
# 云端：在 Streamlit Cloud / HF Spaces 的 Secrets 设置里粘贴同样内容。
#
# 留空或不设置 app_password 时，面板不启用口令（适合本地开发）。

app_password = "改成你的口令"
